package exam.hughwu.cathaytest.common

/**
 * Marker interface for one-time UI events (toast, navigate, dialog).
 *
 * Persistent UI changes belong in [UiState]; this layer is strictly side effects.
 */
interface UiEvent
